apiVersion: v1
kind: Service
metadata:
  name: ca-workflow3
  namespace: sample-app-namespace-1761609201250
  labels:
    app.kubernetes.io/name: ca-workflow3
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ca-workflow3
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000