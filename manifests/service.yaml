apiVersion: v1
kind: Service
metadata:
  name: demo-workflow
  namespace: sample-app-namespace-1752794415339
  labels:
    app.kubernetes.io/name: demo-workflow
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: demo-workflow
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000