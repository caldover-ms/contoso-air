apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: air-workflow
  labels:
    app.kubernetes.io/name: air-workflow
    app.kubernetes.io/part-of: air-workflow-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: air-workflow
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80