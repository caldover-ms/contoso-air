apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: workflow1
  labels:
    app.kubernetes.io/name: workflow1
    app.kubernetes.io/part-of: workflow1-project
    kubernetes.azure.com/generator: devhub
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: workflow1